HTML DOM:
DOM:document object model.
.object representation of all the tags is called DOM.
.DOM will be having tree like structure.
.to change the properties of html with the hep of java script html should be in DOM format.
.if we are calling function with event then no need to give parenthesis.
."this" refers to current object.
.in java script we need to use backgroundColor.
JAVA SCRIPT TIMING FUNCTIONS:
.date() is a function in javascript.
set interval function is always in the need of 2 parameters